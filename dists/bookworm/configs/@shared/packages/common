# This file is input for 'centauripackage --apt --install packages/common' and will
# (de)install the listed packages via debian's apt package manager ...

adcli               install
krb5-user           install
kstart              install
nfs-common          install
openvpn             install
python3-samba       install
samba               install
samba-common-bin    install
samba-vfs-modules   install
smbclient           install
sssd                install
sssd-tools          install
wsdd2               install
