# This file is input for 'centauripackage --apt --install PACKAGE-kde' and will
# (de)install the listed packages via debian's apt package manager ...

desktop-base            install

kdenetwork              install
kdeplasma-addons-data   install
kde-plasma-desktop      install
kde-baseapps            install
kde-standard            install
kompare                 install

plasma-desktop-data     install
plasma-nm               install
plasma-systemmonitor    install
plasma-workspace-data   install

qdirstat                install

kwin-x11                install
sddm                    install
sddm-theme-debian-breeze install

systemsettings          install
