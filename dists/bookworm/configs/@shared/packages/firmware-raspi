# This file is input for 'centauripackage --apt --install packages/firmware-raspi' and will
# (de)install the listed packages via debian's apt package manager ...

# this empty file just excludes the x86 stuff
