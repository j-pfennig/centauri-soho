# This file is input for 'centauripackage --apt --install' and will
# (de)install the listed packages via debian's apt package manager ...

binutils            install
btrfs-progs         install
dosfstools          install
efibootmgr          install
gawk                install
grub-efi-amd64-bin  install
grub-efi-amd64-signed install
openssh-server      install
parted              install
rsync               install
sbsigntool          install
shim-signed         install
systemd-boot        install
tnftp               install
