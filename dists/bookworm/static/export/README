centaurisoho information (can be deleted):

    Folder '/export' contains data that gets exported as files from this host
    It is accessible via serveral network services like samba, ftp or http.   

    On btrfs based installations the folder is a mount-point of a sub-volume.

